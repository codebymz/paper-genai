{
  "name": "paper-genai-backend",
  "version": "1.0.0",
  "description": "Express server backend with Neon DB and Gemini API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "build": "rm -rf dist && cp -r public dist"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
